entities:

  simpleorm.test.manytoone.Owner:
    fields:
      id:
        isId: true
        column: name
      name:
        column: name
      products:
        manyToOne: simpleorm.test.manytoone.Product

  simpleorm.test.manytoone.Product:
    fields:
      id:
        isId: true
        column: id
      name:
        column: name
